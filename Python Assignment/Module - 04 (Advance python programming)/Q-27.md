# What is used to check whether an object o is an instance of class A?

    - You can use the isinstance() function to check if an object is an instance of a particular class:
    - result = isinstance(o, A)